<h1><%= @event.name %></h1>
<strong>Date: <%= @event.date %></strong>
<strong>Host: <%= @event.creator.email %></strong>
<p><%= @event.description %></p>
<%= link_to "Back", root_path %>

<% if user_signed_in? %>
  <%= link_to "Sign Up", attend_event_path(@event.id) %>
<% end %>